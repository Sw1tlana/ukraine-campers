import Features from "../Features/Features";

const CarFeatures = ({ data }) => {

    return (
        <div>
        <Features db={data} />
        </div>
    )
};

export default CarFeatures;
